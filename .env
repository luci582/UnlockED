# Environment variables declared in this file are available at build-time and run-time.
# Next.js + Vite will load these automatically

# Database Configuration
# SQLite database for development (file-based, no setup required)
# DATABASE_URL is now configured directly in schema.prisma
# For production, you can switch back to PostgreSQL

# Alternative PostgreSQL setup (uncomment if needed):
# DATABASE_URL="postgresql://unlocked_user:unlocked_password@localhost:5432/unlocked_db"

# Application Settings
NODE_ENV="development"

# JWT Configuration (for authentication)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# File Upload Configuration
MAX_FILE_SIZE="5MB"
ALLOWED_IMAGE_TYPES="jpg,jpeg,png,webp"

# API Configuration
VITE_API_URL="http://localhost:3001/api"

# Development Settings
ENABLE_QUERY_LOGGING="true"
PRISMA_QUERY_LOG_LEVEL="info"
